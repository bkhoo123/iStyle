import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

export default function SearchBar() {
  return (
    <View>
        {/* to-do: SearchBar -- visible on closet and outfit pages */}
      <Text>SearchBar</Text>
    </View>
  )
}

const styles = StyleSheet.create({})
